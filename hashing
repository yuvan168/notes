hashing - pre-storing and fetching 

in array of numbers 1 2 3 1 2 we need to find count of a particular number appears where to find it we will do for loop but for getting every time we can't do that because the time complexity will O(no of times*N) which makes it a very long time as 10^8=1 sec 10^10=100 sec. so in hashing we prestore the count of numbers in a hasharray and we can get when we need it. this the precalculation or computation

main(){
int n;
n=sc.nextInt();
int arr[n];
for(int i=0;i<n;i++){
cin>>arr[i];
}
int hash[13]={0};
for(int i=0;i<n;i++){
hash[arr[i]]+=1;}

int q;
q=sc.nextInt();
while(q--){
int num;
num=sc.nextInt();
print(hash[num]);
}
return 0;

if arr size is more than 10^5 then if we declare in inside main() then it throws segmentation error so declare globally itgoes till 10^8

int hash={100000000}={0} globally no error

character hashing
int n='a';
it gives 97 ascii value as like that we can declare arr[26] and get the alphabets count as per the alphabet index ch-'a' will be used to get index of the character needed. 
main(){
string s;
s=sc.next();

//pre compute
int hash[26]={0};
for(int i=0;i<s.size();i++){
hash[s[i]-'a']++;}

int q;
q=sc.nextInt();
while(q--){
char c;
sc.next()
print(hash[c-'a']
}

if we dont know the character has upper case or not then arr size should be 256 because total ascii value characters is 256 and we dont want subract a with char c because it will automatically goes to a position where it is needed and we can get it with its index

if in number hashing we need more spac than 10^7 then we can use map to pre compute where we use key value pair which use to store num and count as a pair and it reduces space. for ex,
1 2 3 1 2
2-2
3-1
1-2
which makes only 3 spaces to store rather than storing every count.
here we need to map[arr[i]]++ to check num of count of the number where it check 1 and goes and iterates and gets the count.

int main(){
int n;
n=sc.nextInt();
int arr[n];
for(int i=0;i<n;i++){
arr[i]=sc.nextInt();}

//pre compute
map<int,int>
for(int i=0;i<n;i++){
map[arr[i]]++

int q;
q=sc.nextInt();
while(q--){
int number;
num=sc.nextInt();
print(mpp[number])

map stores everything in sorted order

we can use it in char hashing also and key is not int its still char and its map[arr[str]]++ ++ for frequency

time complexity in map for storing and fetching takes log n as tc in all cases best worst avg
 N- is number of elements in map

for checking sorted order in map

for(auto it:map){
system.println(it.first<<"->"<<it.second)

gives key and value in ordered but unorderedmap gives the in unordered manner

unordered_map<int,int> map; //declare
we use unordered map for tc where O(1) for best avg and O(N) for worst

if array of size more than 10 not allowed use division method

use arr[i]%10 and get the value and assign it to arr as per module value and if same module value is been there then cross chain it as linked list

if all array module value are same then the hash stores everything in one place it makes a collision which has O(N) worst cases in unordered map its a rare case
